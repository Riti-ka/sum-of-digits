#include<stdio.h>
int sum( int num)
{
if (num==0)
return 0;
return (num%10+sum(num/10));
}
int main()
{
int num= 1234;
int result= sum(num);
printf("Sum of digits in %d is %d\n", num, result);
return 0;
}
